# AI 协作协议 - 极速自动化模式 (v2.0)

## ## 1. 状态机同步 (State Machine)
- **启动必读**：每次生成代码前，必须静默读取项目根目录的 `AI_STATUS_SYNC.json`。
- **任务对齐**：仅执行 `next_instruction` 中定义的任务。如果该任务已在 `Gemini_status` 中标记为“通过”，则自动推演执行 Codex 自己在上一轮总结中提出的“后续建议”。
- **禁止废话**：严禁回复“好的”、“我理解了”、“正在准备”。只要 `next_instruction` 存在，第一行输出必须是代码块或文件修改方案。

## ## 2. 代码质量与覆盖 (Full Coverage)
- **禁止省略**：修改函数时必须提供**完整的函数定义**。严禁使用 `// ... rest of code` 或 `# ... 原有代码保持不变`。
- **依赖自检**：如果引入了向量化逻辑（Pandas/Numpy），必须确保文件顶部存在对应的 `import` 语句。
- **注释保留**：必须完整保留原代码中的业务逻辑注释和 `TODO` 标记。

## ## 3. 自动化协作流程 (MCP Loop)
- **审计反馈**：如果 Gemini CLI 在终端输出 `Compliance Check Failed` 或测试报错，Codex 必须立即中断当前思路，优先修复报错，直至 Gemini 输出 `Test Passed`。
- **性能达标制**：如果 `next_instruction` 设定了性能指标（如“跑进 10s”），Codex 在修改后必须主动编写或更新对应的 `benchmark` 脚本并请求 Gemini 运行。

## ## 4. 行为红线 (Red Lines)
- **环境隔离**：严禁修改 `.env`、`credentials.json` 或任何网络代理配置（由 Gemini 负责）。
- **无感同步**：严禁在代码中加入任何弹出式对话框（如 `input()` 或回写 Google Sheets 的确认弹窗）。所有输出必须静默写入 `./dist/` 文件夹。

## ## 5. 触发下一步
- **主动权交还**：完成代码修改后，必须在对话末尾明确提示：“逻辑已更新，请 Gemini CLI 运行审计并更新里程碑。”
