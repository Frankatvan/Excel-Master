# AiWB Budget Cost Change Order 候选映射 v0.1

日期：`2026-04-17`  
对象：`109` 中 `Budget Cost Change Order`

## 1. 109 当前观测值
1. 2023: `+2,175.810493`
2. 2024: `-69,283.242176`
3. 2026: `0`
4. Total: `-67,107.431683`

## 2. 反查日志结果（当前快照）
### 2.1 Change Order Log（Approved Change Orders）
按 `Code` 汇总（2025）：
1. `66009104`: `-510,408.03`（Vertical Cost Contingency-GC）
2. `66009101`: `-27,170.25`（Soft Cost Contingency-Owner）
3. 正向施工类（如 `63040000/63220000/63260000...`）为正值。

结论：`66009101/66009104` 是最强候选代码组。

### 2.2 Transfer Log（Total）
按 `Code` 年度汇总：
1. `66009104`: `2024=-61,872.450485`, `2025=-37,510.96`
2. `66009101`: `2024=0`, `2025=-21,466.90`

结论：`2024` 年 `66009104` 的量级与 `109` 的 `-69,283.242176` 接近。

## 3. 候选映射策略（待最终确认）
### 策略 A（优先）
1. 基础来源：`Change Order Log.Approved Change Orders`
2. 调整来源：`Transfer Log` 中 Contingency 相关 code（`66009101/66009104`）
3. 映射口径：按期间得出 `BudgetCO_t`（当期变动），再累计进 `EAC_t`

### 策略 B（保守）
1. `BudgetCO_t` 暂由人工维护
2. 同时跑日志校核报表（仅预警，不自动覆盖）
3. 当校核通过后再切自动写入

## 4. 为什么当前还不能全自动落锤
1. 日志主数据在当前快照中集中于 2025，而 `109` 目标值主要落在 2023/2024 列。
2. 需要确认 `109` 年列与日志日期列的期间映射规则（自然年/财年/项目阶段年）。
3. 需明确是否存在“审计调账”优先覆盖日志净额的规则。

## 5. 落地建议
1. 先在代码里保留 `BudgetCO_t` 手工入口。
2. 并行输出“日志建议值”与“手工值”差异。
3. 差异为 0 连续通过后，切换为自动模式。
